/**
 * @param className - ClassNames to be passed to the component
 * @param darkMode - whether to turn on darkMode or not
 */

export interface LogoProps {
  className?: string;
  darkMode?: boolean;
}
